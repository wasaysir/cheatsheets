First slideshow: What is Scala

Scala doesn't automatically convert a float to an int, you need to manually call the toInt method.

There are modifying assignment operators, like +=, /=, etc. But generally you shouldn't be doing much assignment.

Java is very verbose because its reflection system isn't great, so you need to be very verbose. 

Slide 9 - Collections
