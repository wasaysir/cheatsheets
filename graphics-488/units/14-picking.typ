= Picking
When we enter picking mode, draw the scene with shading turned off and each object given a unique colour. When done reading, read the pixel selected and lookup the colour to determine which object was selected.

In A3 we used a separate back buffer to achieve this.
